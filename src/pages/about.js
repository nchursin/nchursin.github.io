import * as React from "react"

import { Layout } from '../templates'

// markup
export default () => {
  return (
    <Layout>
      <title>About</title>
      <h1>
        About
      </h1>
    </Layout>
  )
}
