import * as React from "react"

import { Layout } from '../templates'

// markup
export default () => {
  return (
    <Layout>
      <title>Projects</title>
      <h1>
        Projects
      </h1>
    </Layout>
  )
}
